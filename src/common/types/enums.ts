export enum ERoles {
  USER = 'user',
  ADMIN = 'admin',
  MODERATOR = 'moderator',
}

export enum EFileType {
  JPEG = 'jpeg',
  PNG = 'png',
  JPG = 'jpg',
  PDF = 'pdf',
}

export enum EBucket {
  BLOG_IMAGES = 'blog-images-bucket2',
}
